{
  "name": "Mixable",
  "version": "0.2.0",
  "description": "Listen with friends",
  "main": "index.js",
  "scripts": {
    "dist": "build",
    "pack": "build --dir",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Noah Zoschke",
  "license": "ISC",
  "dependencies": {
    "@jonny/spotify-web-helper": "^1.5.0",
    "firebase": "^3.6.1"
  },
  "devDependencies": {
    "electron": "^1.4.1"
  },
  "build": {
    "appId": "net.mixable.app",
    "mac": {
      "category": "your.app.category.type"
    },
    "win": {
      "iconUrl": "(windows-only) https link to icon"
    }
  }
}
